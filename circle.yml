dependencies:
  pre:
    - go get github.com/spf13/hugo
    - go install github.com/spf13/hugo

test:
  override:
    - /bin/true

deployment:
  production:
    branch: master
    commands:
      - ./site/s3sync.sh
